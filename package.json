{
  "name": "insert-coin",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "lint": "eslint src public/sw.js scripts && node scripts/checkhandlers.mjs",
    "build": "npm run lint && vite build && node scripts/publish.mjs",
    "build:preview": "npm run lint && vite build && node scripts/publish-preview.mjs",
    "test": "npm run lint && vitest run",
    "test:watch": "vitest",
    "check:imports": "node scripts/checkimports.mjs"
  },
  "devDependencies": {
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
