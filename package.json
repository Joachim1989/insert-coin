{
  "name": "insert-coin",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/publish.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:imports": "node scripts/checkimports.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
